export default {
  plugins: {
    // Tailwind v4 ships as a dedicated PostCSS plugin and handles
    // vendor prefixing internally (autoprefixer no longer required).
    '@tailwindcss/postcss': {},
  },
};
